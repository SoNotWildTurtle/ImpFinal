# IMP Model Compatibility Directory

The canonical runtime model directory lives under `imp/models/`.

This top-level `models/` directory exists as a compatibility marker for repo-level notes, audits, and operator guidance that refer to `models/` from the repository root.

If you add offline GGUF assets for runtime use, place them under `imp/models/` unless a specific tool explicitly targets the top-level directory.
